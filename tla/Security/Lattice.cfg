\* CTRLMATRIX M4 v0.2 -- TLC config for Lattice.tla

SPECIFICATION Spec_M4

CONSTANTS
    Sigma_C            = {"pub", "secret"}
    Sigma_I            = {"trust", "untrust"}
    Sigma_P            = {"web", "user"}
    Principal          = {"p1", "p2"}
    Transformers       = {"Identity", "DropWeb"}
    MaxEvents          = 2
    TransformerFn      <- DefaultTransformerFn
    AuthRel            <- DefaultAuthRel
    MintingTrusted     <- DefaultMintingTrusted
    NoOpTransformers   <- DefaultNoOpTransformers

INVARIANTS
    M4_Inv
    LabelFlowExtensibility

CHECK_DEADLOCK FALSE
